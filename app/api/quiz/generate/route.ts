// Placeholder — quiz generation via static bank (Phase 1)
// See /api/quiz/session for the active route
export async function POST() {
  return Response.json({ message: 'Use /api/quiz/session for Phase 1 static bank' }, { status: 200 });
}
